<div class="center">
  <% if !@correct_num_cols %>
    <h1>Parsing Error</h1>
    <p>The number columns in the uploaded table is incorrect. Please check row <b><%= @row %></b> it has <%= @col %> columns in your table. It is supposed to have <%= @numCols %> columns.</p>
  <% elsif @valid %>
    <h2>Please confirm this info</h2>
    <table class="pure-table">
      <thead>
        <tr>
          <th>UIN</th>
          <th>First Name</th>
          <th>Last Name</th>
          <th>Email</th>
          <th>Committee</th>
          <th>Subcommittee</th>
          <th>Point Total</th>
          <th>Meeting Points</th>
          <th>Event Points</th>
          <th>Misc Points</th>
          <th>Admin</th>
        </tr>
      </thead>
      <tbody>
        <% @users.each  do |user| %>
          <tr>
            <td><%= user.uin %></td>
            <td><%= user.first_name %></td>
            <td><%= user.last_name %></td>
            <td><%= user.email %></td>
            <td><%= user.committee %></td>
            <td><%= user.subcommittee %></td>
            <td><%= user.total_points %></td>
            <td><%= user.meeting_points %></td>
            <td><%= user.event_points %></td>
            <td><%= user.misc_points %></td>
            <td><%= user.admin %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <a class="button" href="/bulk_add_users/confirmed?filename=<%= @file %>"> Confirm</a>
  <% else %>
    <h1>Parsing Error</h1>
    <% @users.each  do |user| %>
      <% if user.errors.messages != {}%>
        <p>For member with the UIN: <b><%= user.uin %></b> the first name: <b><%= user.first_name %></b> and the last name: <b><%= user.last_name%></b> the following errors occured: </p>
        <ul>
          <% user.errors.messages.each do |error|%>
            <li> the field <%= error[0] %> <%= error[1][0] %> </li>
          <% end %>
        </ul>
      <% end %>
    <% end %>
    <p>Please find this user in the table and ensure that there info is input correctly</p>
  <% end %>
</div>
